# PHP คืออะไร

php : ทำงานบน Server ก่อนเสมอ


### Operator

&& -> เช็คซ้าย
& -> เช็คขวา

### Object

```
const user = {
    fname: "Foo",
    lname: "bar",
    fullname: function () {
        return `${fname} ${lname}`
    }

}
```